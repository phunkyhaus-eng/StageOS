[build]
dockerfilePath = "apps/api/Dockerfile"
watchPatterns = [
  "apps/api/**",
  "packages/shared/**",
  "pnpm-lock.yaml",
  "pnpm-workspace.yaml",
  "package.json"
]

[deploy]
startCommand = "node dist/apps/api/src/worker.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
